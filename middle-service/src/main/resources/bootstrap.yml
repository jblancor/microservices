    server:
      port: ${MIDDLE_PORT:8090}
     
    spring:
      application:
        name: middle-service
      cloud:
        config:
          discovery:
            enabled: true
            service-id: config-server


